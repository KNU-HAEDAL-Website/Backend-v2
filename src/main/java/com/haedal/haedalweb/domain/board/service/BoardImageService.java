package com.haedal.haedalweb.domain.board.service;

import com.haedal.haedalweb.domain.board.model.BoardImage;

public interface BoardImageService {
    void removeBoardImage(BoardImage boardImage);
}
